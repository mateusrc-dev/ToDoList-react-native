import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  checkbox: {
    width: 24,
    height: 24,
    borderRadius: 9999,
    borderColor: '#4EA8DE',
    backgroundColor: 'transparent',
  },
});
